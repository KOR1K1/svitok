<?xml version="1.0" encoding="utf-8"?>
<!-- Строка-фолбек автозаполнения (когда клавиатура не поддерживает inline):
     скруглённая поверхность, иконка Свитка, текст шрифтом Golos. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:background="@drawable/autofill_row_bg"
    android:paddingLeft="16dp"
    android:paddingRight="16dp"
    android:paddingTop="13dp"
    android:paddingBottom="13dp">
    <ImageView
        android:layout_width="28dp"
        android:layout_height="28dp"
        android:layout_marginEnd="12dp"
        android:src="@mipmap/ic_launcher"
        android:contentDescription="@string/app_name" />
    <TextView
        android:id="@+id/af_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:fontFamily="@font/golos"
        android:textColor="@color/ink_text"
        android:textSize="15sp" />
</LinearLayout>
