The QR scanner is now our own (CameraX + ZXing) - Google ML Kit and its telemetry leave the APK entirely. New scan UX: viewfinder frame, in-frame detection only, lock-on that tracks the code. Desktop entry screens redone; show/hide is an eye inside the field. Plus everything from 0.3.x: multiple accounts per domain, domain aliases, import from other managers, reproducible builds, and the INTERNET permission hotfix.
