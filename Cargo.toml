[workspace]
members = ["core", "common", "cli", "app/src-tauri", "host"]
resolver = "2"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
# в криптокоде переполнения идут через wrapping_*, так что проверки ловят только настоящие баги
overflow-checks = true
panic = "abort"
strip = true
